@{
  Layout = "_Layout";
}

<h3>Hello</h3>

@* @using Registrar.Models;
@model List<Registrar.Models.Department>;

<h2>Departments Page</h2>

@if (@Model.Count == 0)
{
  <h3>This university has no departments.</h3>
}


@foreach (Department department in Model)
{
  <li>@Html.ActionLink($"{department.Name}", "Details", new { id = department.DepartmentId })</li>
}

<p>@Html.ActionLink("Add a new Department", "Create")</p>

<p>@Html.ActionLink("Home", "Index", "Home")</p> *@